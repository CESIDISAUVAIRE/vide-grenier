# Environnement de PROD — utilisé par docker-compose.prod.yml
# La connexion se fait via le NOM DU SERVICE Docker `db`, jamais localhost.
DATABASE_URL=postgresql://videgrenier:videgrenier@db:5432/videgrenier